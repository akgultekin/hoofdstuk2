const userName = prompt('Wat is jouw naam?', 'User');
alert(userName);
const allowCookies = confirm('Wilt u cookies bewaren?')

const myCity = 'Den Haag';
alert('Ik woon in ' + myCity);

if (allowCookies) {
    console.log(allowCookies);
} else {
    console.log(allowCookies);
}